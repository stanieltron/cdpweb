
<div class="button-view">
    <p>Share youe success with your co-holders</p>
    <p></p>
    <p class="descr">This is a function for project owners, to share revenue with all it's token holders. Every funding will add to total funds, for all the tokens. Funds per one token are calculated based on total allocation of tokens</p>
    <p style="padding-top: 30px;">fund this enterprise with <span><input type="number"  pattern="^\d*(\.\d{0,2})?$" id="fund-input" value="0" step="0.00001"/> </span> ETH </span></p>
    <p style="padding: 30px;">
        <a id="fund-execute-button" href="#" class="button">let's do it</a>
    </p>
</div>


<script src="scriptsFund.js"></script>
<script>
    // Create an object to store token details mapped to token addresses
    window.tokenDetails = {
        tokenAddress: '<%= tokenDetails.tokenAddress %>',
        registryAddress: '<%= tokenDetails.registryAddress %>',
        name: '<%= tokenDetails.name %>',
        symbol: '<%= tokenDetails.symbol %>',
        decimals: '<%= tokenDetails.decimals %>',
        logoPath: '<%= tokenDetails.logoPath %>',
        totalFunds: '<%=  tokenDetails.totalFunds %>',
        totalSupply: '<%=  tokenDetails.totalSupply %>',
    }
</script>